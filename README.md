# To Do List - Lerna, Next.js(React), Storybook

Playing around with:

- Lerna
- Monorepo
- Next.js (React)
- Storybook
- Emotion.js
- For Lerna: npm install --global lerna > lerna bootstrap > lerna run dev --parallel . View on localhost:3000
- For Storybook Components: cd packages && yarn storybook . View on localhost:6006
- lerna exec npm install
